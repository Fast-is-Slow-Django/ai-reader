# 贡献指南

感谢你对 AI-Reader 的关注！我们欢迎任何形式的贡献。

## 📝 如何贡献

### 报告 Bug

如果你发现了 Bug，请：

1. 检查 [Issues](https://github.com/YOUR_USERNAME/ai-reader/issues) 是否已有相关问题
2. 如果没有，创建新的 Issue，包含：
   - 详细的问题描述
   - 复现步骤
   - 预期行为 vs 实际行为
   - 截图（如果适用）
   - 环境信息（浏览器、操作系统等）

### 提交功能建议

我们欢迎新功能建议！请：

1. 创建新的 Issue，标题以 `[Feature Request]` 开头
2. 详细描述：
   - 功能的用途
   - 使用场景
   - 可能的实现方式

### 提交代码

1. **Fork 项目**
   ```bash
   # 在 GitHub 上点击 Fork 按钮
   ```

2. **克隆你的 Fork**
   ```bash
   git clone https://github.com/YOUR_USERNAME/ai-reader.git
   cd ai-reader
   ```

3. **创建新分支**
   ```bash
   git checkout -b feature/your-feature-name
   # 或
   git checkout -b fix/bug-description
   ```

4. **进行修改**
   - 遵循现有代码风格
   - 添加必要的注释
   - 确保 TypeScript 类型正确

5. **测试你的修改**
   ```bash
   npm run dev
   # 手动测试你的修改
   ```

6. **提交代码**
   ```bash
   git add .
   git commit -m "feat: add amazing feature"
   # 或
   git commit -m "fix: resolve issue #123"
   ```

   **Commit 消息格式：**
   - `feat:` 新功能
   - `fix:` Bug 修复
   - `docs:` 文档更新
   - `style:` 代码格式（不影响功能）
   - `refactor:` 重构
   - `test:` 测试相关
   - `chore:` 构建/工具相关

7. **推送到你的 Fork**
   ```bash
   git push origin feature/your-feature-name
   ```

8. **创建 Pull Request**
   - 在 GitHub 上点击 "New Pull Request"
   - 填写 PR 描述，说明你的修改
   - 关联相关 Issue（如果有）

## 💻 开发指南

### 项目结构

```
ireader/
├── app/                    # Next.js App Router
│   ├── api/               # API 路由
│   ├── dashboard/         # 书架页面
│   ├── login/             # 登录页面
│   └── reader/            # 阅读器页面
├── components/            # React 组件
├── utils/                 # 工具函数
└── types/                 # TypeScript 类型
```

### 代码规范

- ✅ 使用 TypeScript
- ✅ 使用函数式组件和 Hooks
- ✅ 遵循 ESLint 规则
- ✅ 使用有意义的变量名
- ✅ 添加必要的注释（中文或英文）
- ✅ 保持代码简洁

### 命名约定

- **组件**: PascalCase (`BookUploader.tsx`)
- **函数**: camelCase (`loadBookData()`)
- **常量**: UPPER_SNAKE_CASE (`MAX_FILE_SIZE`)
- **文件**: kebab-case 或 PascalCase

### 添加新功能

1. **阅读器功能**: 修改 `components/reader/DirectEpubReader.tsx`
2. **书架功能**: 修改 `app/dashboard/page.tsx`
3. **API 路由**: 添加到 `app/api/`
4. **工具函数**: 添加到 `utils/`

### 样式指南

- 使用 Tailwind CSS
- 遵循移动优先设计
- 保持响应式布局
- 使用 Lucide React 图标

## 🧪 测试

目前项目使用手动测试。请确保：

- ✅ 功能在 Chrome、Safari、Firefox 上正常工作
- ✅ 移动端适配正常
- ✅ 没有控制台错误
- ✅ 不破坏现有功能

## 📚 文档

如果你的 PR 添加了新功能：

- 更新 `README.md`
- 添加/更新相关文档到 `docs/`
- 添加代码注释

## ✅ PR 检查清单

提交 PR 前，请确认：

- [ ] 代码遵循项目规范
- [ ] 已测试所有修改
- [ ] 没有 TypeScript 错误
- [ ] 已更新相关文档
- [ ] Commit 消息清晰明确
- [ ] PR 描述详细

## 🆘 需要帮助？

- 查看 [文档](./docs/)
- 查看现有 [Issues](https://github.com/YOUR_USERNAME/ai-reader/issues)
- 创建新的 Discussion

## 📜 行为准则

- 尊重所有贡献者
- 保持友好和专业
- 接受建设性批评
- 专注于项目目标

---

再次感谢你的贡献！🎉
